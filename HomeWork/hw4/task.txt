ƒомашнее задание:
–еализовать 3 сервиса
1. TextService - API текстов:
 а) сохранение текстового документа (бинарный файл)
б) сохранение текста в виде строки 
в) сохранение текста по url текстового файла (файл по указанному url выкачивает и сохран€ет сам сервис) 
(во всех случв€х пока сохран€ть в локальное хранилище, например в List или Dictionary, потом переведем на базу данных)
г) получение текстов по Id
д) получение всех текстов (спосок)
2. FindService API - поиск по текстовому файлу. ћетод API которому передаетс€ id текста и набор слов которые необходимо искать в указанном файле. 
3. TaskService  - 
а) API метод, в котором реализуетс€ постановка задачи на обработку файлов. ¬ задаче указываетс€ начало(дата), окончание(дата), периодичность (в минутах) проверки наличи€ новых файлов, поисковые слова. TaskService так же записывает результаты обработки дл€ каждого файла в локальное хранилище , например в List или Dictionary, потом переведем на базу данных) в виде id текста, id задачи, количестов найденных слов .  аждый файл обрабатываетс€ только 1 раз
б) получение результатов задачи по Id задачи

»того: 3 сервиса, TextService (с набором методов API), FindService(1 метод), TaskService (2 метода API), продумать схему их взаимодействи€.